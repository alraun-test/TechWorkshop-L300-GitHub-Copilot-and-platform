# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json
name: zavastorefront
metadata:
  template: zavastorefront@0.0.1-beta

infra:
  provider: bicep
  path: ./infra

services:
  web:
    project: ./src
    language: csharp
    host: appservice
    docker:
      path: ./Dockerfile
      context: .
      registry: fromEnvironment
